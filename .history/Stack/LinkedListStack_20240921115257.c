#include <stdio.h>
#include <stdlib.h>

#define ele_type int

typedef struct Node {
  ele_type data;
  struct  N
  {
    /* data */
  };
  
}
typedef struct Stack {
  ele_type element
}